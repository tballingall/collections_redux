= form_for [@album.user, @album] do |f|
  = render partial: 'shared/error_messages', locals: {model: @album}
  = f.label :name
  = f.text_field :name, class: 'form-control'
  %br
  = f.submit "Submit", class: "btn btn-primary"